%div.row
  %div.col-xs-6= candidate.name
  %div.col-xs-4= candidate.patrol.name
  %div.col-xs-2= link_to 'Add', add_event_attendance_path(@event.id, candidate.id), :remote => true, class: "btn btn-primary"